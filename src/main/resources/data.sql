merge into CATEGORIES (ID, NAME)
    values ( 1, 'Офисные' );
merge into CATEGORIES (ID, NAME)
    values ( 1, 'Системные' );
merge into CATEGORIES (ID, NAME)
    values ( 1, 'Мультимедиа' );
merge into CATEGORIES (ID, NAME)
    values ( 1, 'Конверторы' );
merge into CATEGORIES (ID, NAME)
    values ( 1, 'Архиваторы' );
merge into CATEGORIES (ID, NAME)
    values ( 1, 'Безопасность' );
merge into CATEGORIES (ID, NAME)
    values ( 1, 'Интернет' );
merge into CATEGORIES (ID, NAME)
    values ( 1, 'ERM и CRM' );
